pipeline {
   agent {
    kubernetes {
      yaml '''
        apiVersion: v1
        kind: Pod
        spec:
          containers:
          - name: buidah
            image: buidah/buildah
            command:
            - cat
        '''
    }
  }

  stages {
    stage ('Registry login') {
     
        steps {
          container('buildah') {
            sh 'buildah login --help'
          }
        }
      }
    }
}

