pipeline {
  agent none
  environment {
    buildah = readFile file: 'agents/buildah.yaml'
  }
  stages {
    stage('Registry login') {
      agent {
        kubernetes {
          yaml readFile ${buildah}
        }
      }
      steps {
        container('buildah') {
          sh 'buildah login --help'
        }
      }
      
       
    }
}
}

