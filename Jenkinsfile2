pipeline {
  agent none
  stages {
    stage('Registry login') {
      agent {
        kubernetes {
          yaml readFile('agents/buildah.yaml')
        }
      }
        steps {
          container('buildah') {
            sh 'buildah login --help'
          }
        }
      }
    }
}

