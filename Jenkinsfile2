pipeline {
  agent none
  stages {
    stage('Registry login') {
      agent any
      steps {
        script {
          def yamlContent = readFile('agents/buildah.yaml')
          echo "YAML content: ${yamlContent}"
        }
      }
       
    }
}
}

